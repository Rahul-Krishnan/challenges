/* jshint esversion: 6 */
alterContext = (fn, obj) => {
  // Optional Challenge
};

alterObjects = (constructor, greeting) => {
  // Optional Challenge
};

iterate = (obj) => {
  // Optional Challenge
};
